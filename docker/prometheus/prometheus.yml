global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'crypto-volatility'
    environment: 'development'

# Alerting configuration
rule_files:
  - '/etc/prometheus/alerts.yml'

scrape_configs:
  - job_name: 'volatility-api'
    static_configs:
      - targets: ['api:8000']
        labels:
          service: 'volatility-api'
          job: 'api'
    metrics_path: '/metrics'
    scrape_interval: 5s

  # Disabled: These services don't expose metrics endpoints
  # - job_name: 'feature-pipeline'
  #   static_configs:
  #     - targets: ['featurizer:8001']
  #       labels:
  #         service: 'feature-pipeline'
  #         job: 'consumer'
  #   metrics_path: '/metrics'
  #   scrape_interval: 10s

  # - job_name: 'prediction-consumer'
  #   static_configs:
  #     - targets: ['prediction-consumer:8002']
  #       labels:
  #         service: 'prediction-consumer'
  #         job: 'consumer'
  #   metrics_path: '/metrics'
  #   scrape_interval: 10s

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

